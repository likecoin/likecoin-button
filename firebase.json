{
  "functions": {
    "source": "functions/dist",
    "predeploy": [
      "npm --prefix src run lint && npm --prefix src run build && cp src/nuxt.config.js functions/ && npm --prefix functions run lint && npm --prefix functions run package "
    ]
  },
  "hosting": {
    "predeploy": [
      "rm -rf public/ && cp -r functions/nuxt/dist/ public/"
    ],
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "function": "ssrapp"
      }
    ]
  }
}
